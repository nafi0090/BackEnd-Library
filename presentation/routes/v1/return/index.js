const express = require('express');
const router = express.Router();
const RETURN_CONTROLLER = require('../../../controller/return.controller');

// router.get('/', RETURN_CONTROLLER.index);

module.exports = router;